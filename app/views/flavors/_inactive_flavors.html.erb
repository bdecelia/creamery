<% if can? :update, Flavor %>
<% unless @inactive_flavors.empty? %>
  <div class="panel callout radius">
    <h3>Inactive Flavors</h3>
    <ul>
      <% @inactive_flavors.each do |flavor| %>
        <li><%= link_to flavor.name, flavor %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<% end %>